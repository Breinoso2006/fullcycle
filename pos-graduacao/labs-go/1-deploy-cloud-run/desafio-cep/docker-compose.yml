services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - weatherKey=${WEATHER_API_KEY:-api-key}
    restart: unless-stopped
